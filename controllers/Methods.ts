/**
 * @readonly
 * @enum {string}
 * Represents CRUD methods used by the controller. Used in conjunction with {@link IRoute} to specify the method of an endpoint object.
 */
export enum Methods {
  GET = 'GET',
  POST = 'POST',
  PUT = 'PUT',
  DELETE = 'DELETE',
}
